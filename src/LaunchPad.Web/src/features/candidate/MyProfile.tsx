import { Title2 } from '@fluentui/react-components';

// Candidate's own profile: skills, resume upload (SAS-issued), assignment status.
export function MyProfile() {
  return <Title2>My Profile</Title2>;
}
