# Copy to .env (gitignored) before running docker-compose.homelab.yml /
# scripts/run-local-full.sh. This is a throwaway password for a local
# container only — never a real credential, never committed.
MSSQL_SA_PASSWORD=LaunchPad_Local_2026!
